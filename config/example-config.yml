# Database credentials
database:
  host: "localhost"
  port: 5432
  name: "<your-db-name>"
  user: "postgres"
  pass: "<your-db-pass>"
  timezone: "<your-db-timezone>"
  connections: 10

# Server configurations
server:
  host: "0.0.0.0"
  port: 8080
